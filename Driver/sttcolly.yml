info:
  name: sttcolly
  type: http
  seq: 14

http:
  method: GET
  url: "{{baseurl}}/api/driver/stts/:sttNumber/kolis?tripId=TRP-001089&manifestId=MFT-0000004232"
  params:
    - name: tripId
      value: TRP-001089
      type: query
    - name: manifestId
      value: MFT-0000004232
      type: query
    - name: sttNumber
      value: A0018274
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
