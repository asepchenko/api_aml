info:
  name: Status — Arrived (legacy)
  type: http
  seq: 8

http:
  method: PUT
  url: "{{base_url}}/driver/pickup-retur/{{pickup_retur_id}}/status"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "action": "arrived",
        "city_name": "Jakarta",
        "last_location": "Gudang HO Jakarta"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Legacy — prefer POST arrive-ho
