info:
  name: Status — Start Trip (assigned → on_trip)
  type: http
  seq: 3

http:
  method: PUT
  url: "{{base_url}}/driver/pickup-retur/{{pickup_retur_id}}/status"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "action": "start_trip"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  SP: sp_driver_pickup_retur_status_json

  Insert order_trackings: On Process Delivery
