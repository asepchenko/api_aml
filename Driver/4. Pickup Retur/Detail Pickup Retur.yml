info:
  name: Detail Pickup Retur
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/driver/pickup-retur/{{pickup_retur_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  SP: sp_driver_pickup_retur_detail_json

  next_action: start_trip | arrived
