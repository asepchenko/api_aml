info:
  name: profile
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseurl}}/api/driver/profile"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
