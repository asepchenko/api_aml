info:
  name: Detail Pickup Retur
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseurl}}/api/driver/pickup-retur/RET-2605300001"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  SP: sp_driver_pickup_retur_detail_json

  next_action: start_trip | arrived
