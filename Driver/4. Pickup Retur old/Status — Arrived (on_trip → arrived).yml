info:
  name: Status — Arrived (on_trip → arrived)
  type: http
  seq: 6

http:
  method: PUT
  url: "{{baseurl}}/api/driver/pickup-retur/RET-2605300001/status"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "action": "arrived"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  SP: sp_driver_pickup_retur_status_json

  Insert order_trackings: Transit (kota cabang HO)
