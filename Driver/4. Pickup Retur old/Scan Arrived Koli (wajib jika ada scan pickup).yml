info:
  name: Scan Arrived Koli (wajib jika ada scan pickup)
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseurl}}/api/driver/pickup-retur/RET-2605300001/scan/arrived"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "sttNumber": "TESTIT10",
        "koliId": "TESTIT10-1"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "SP: sp_driver_pickup_retur_scan_koli_json (phase=arrived)"
