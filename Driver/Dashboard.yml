info:
  name: Dashboard
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseurl}}/api/driver/dashboard"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
