info:
  name: pickup_accept
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseurl}}/api/driver/pickup/:id/accept"
  headers:
    - name: Content-Type
      value: application/json
  params:
    - name: id
      value: PU-2025-000003
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
