info:
  name: pickuplist
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseurl}}/api/driver/pickup?page=1&limit=10"
  params:
    - name: status
      value: ""
      type: query
      disabled: true
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
