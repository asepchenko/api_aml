info:
  name: pickup
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseurl}}/api/driver/pickup/9"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
