info:
  name: packages
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseurl}}/api/driver/packages"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
