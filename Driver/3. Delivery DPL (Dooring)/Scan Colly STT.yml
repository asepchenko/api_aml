info:
  name: Scan Colly STT
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseurl}}/api/driver/delivery/56/scan/koli"
  body:
    type: json
    data: |-
      {
        "sttNumber": "A0038302",
        "koliId": "A0038302-1"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
