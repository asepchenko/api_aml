info:
  name: Detail STT Kolis
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseurl}}/api/driver/delivery/56/stts/A0038302/kolis"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
