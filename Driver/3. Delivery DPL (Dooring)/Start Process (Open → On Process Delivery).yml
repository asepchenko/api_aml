info:
  name: Start Process (Open → On Process Delivery)
  type: http
  seq: 1

http:
  method: PUT
  url: "{{baseurl}}/api/driver/delivery/46/start-process"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "SP: sp_driver_delivery_start_process_json"
