info:
  name: Detail Delivery DPL
  type: http
  seq: 18

http:
  method: GET
  url: "{{baseurl}}/api/driver/delivery/32"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  SP: sp_driver_delivery_detail_json

  Response: delivery.next_action = start_process | deliver_items
