info:
  name: manifeststt
  type: http
  seq: 12

http:
  method: GET
  url: "{{baseurl}}/api/driver/manifests/:manifestId/stts"
  params:
    - name: manifestId
      value: MFT-0000004011
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
