info:
  name: Login
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseurl}}/api/auth/login"
  body:
    type: json
    data: |-
      {
          "username" : "yudhimahelfin@gmail.com",
          "password" : "password",
          "platform" : "android"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
