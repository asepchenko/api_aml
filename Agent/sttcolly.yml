info:
  name: sttcolly
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseurl}}/api/agent/stts/:sttNumber/kolis"
  params:
    - name: sttNumber
      value: A0018274
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
