info:
  name: dashboard
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseurl}}/api/agent/dashboard"
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
