info:
  name: scanstt
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseurl}}/api/agent/scan/koli"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{\r

      \  \"sttNumber\": \"A0018274\",\r

      \  \"koliId\": \"A0018274-1\"\r

      }"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
