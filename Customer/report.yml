info:
  name: report
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseurl}}/api/customer/reports"
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
