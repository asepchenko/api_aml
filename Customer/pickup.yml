info:
  name: pickup
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseurl}}/api/customer/pickup/1"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
