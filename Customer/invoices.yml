info:
  name: invoices
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseurl}}/api/customer/invoice"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
