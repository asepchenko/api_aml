info:
  name: order
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseurl}}/api/customer/orders?status=Delivered&start_date=2026-01-01&endt_date=2026-01-31&page=1&limit=10"
  params:
    - name: status
      value: Delivered
      type: query
    - name: start_date
      value: 2026-01-01
      type: query
    - name: endt_date
      value: 2026-01-31
      type: query
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
