info:
  name: dashboard
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseurl}}/api/customer/dashboard"
  body:
    type: multipart-form
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
