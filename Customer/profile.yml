info:
  name: profile
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseurl}}/api/customer/profile"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
