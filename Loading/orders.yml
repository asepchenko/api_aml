info:
  name: orders
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseurl}}/api/loading/orders"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
