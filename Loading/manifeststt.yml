info:
  name: manifeststt
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseurl}}/api/loading/manifests/:manifestId/stts"
  params:
    - name: manifestId
      value: MFT-0000003498
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
